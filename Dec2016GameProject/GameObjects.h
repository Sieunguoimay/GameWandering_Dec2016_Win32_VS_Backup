#pragma once
#include"globalheader.h"
#include"Camera.h"
#ifndef GAMEOBJECTS_H
#define GAMEOBJECTS_H

class GameObjects
{
public:
	GameObjects();
	~GameObjects();
	//virtual void display(const Camera&camera) = 0;
};

#endif // !GAMEOBJECTS_H
